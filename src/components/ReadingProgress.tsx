"use client";

// ponytail: pure CSS scroll-driven progress, zero JS. Unsupported browsers hide it.
export function ReadingProgress() {
  return <div aria-hidden="true" className="reading-progress" />;
}
