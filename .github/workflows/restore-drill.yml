name: Production restore drill

on:
  schedule:
    - cron: "15 4 1 * *"
  workflow_dispatch:

concurrency:
  group: production-restore-drill
  cancel-in-progress: false

jobs:
  restore-drill:
    name: Restore latest backup in isolation
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install restore dependencies
        run: sudo apt-get update && sudo apt-get install -y sqlite3

      - name: Configure SSH
        env:
          DEPLOY_HOST: ${{ vars.DEPLOY_HOST }}
          DEPLOY_SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          DEPLOY_SSH_PASSPHRASE: ${{ secrets.DEPLOY_SSH_PASSPHRASE }}
        run: |
          test -n "$DEPLOY_SSH_PRIVATE_KEY"
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          eval "$(ssh-agent -s)"
          echo "SSH_AUTH_SOCK=$SSH_AUTH_SOCK" >> "$GITHUB_ENV"
          echo "SSH_AGENT_PID=$SSH_AGENT_PID" >> "$GITHUB_ENV"
          if [ -n "${DEPLOY_SSH_PASSPHRASE:-}" ]; then
            cat > "$RUNNER_TEMP/ssh-askpass" <<'EOF'
          #!/usr/bin/env sh
          printf '%s\n' "$DEPLOY_SSH_PASSPHRASE"
          EOF
            chmod 700 "$RUNNER_TEMP/ssh-askpass"
            DISPLAY=none SSH_ASKPASS="$RUNNER_TEMP/ssh-askpass" SSH_ASKPASS_REQUIRE=force ssh-add ~/.ssh/deploy_key </dev/null
          else
            ssh-add ~/.ssh/deploy_key
          fi
          DEPLOY_HOST="${DEPLOY_HOST:-72.60.195.46}"
          if [ "$DEPLOY_HOST" = "72.60.195.46" ]; then
            printf '%s\n' '72.60.195.46 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIPW6DVc3HbWdUhmPpQxR2De5SrdmSsmYyxm+RrHgedSl' >> ~/.ssh/known_hosts
          else
            ssh-keyscan -T 30 -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts
          fi
          cat > ~/.ssh/config <<EOF
          Host $DEPLOY_HOST
            IdentityFile ~/.ssh/deploy_key
            IdentityAgent $SSH_AUTH_SOCK
            IdentitiesOnly yes
            StrictHostKeyChecking yes
            ConnectTimeout 30
            ConnectionAttempts 1
            ServerAliveInterval 15
            ServerAliveCountMax 3
          EOF
          chmod 600 ~/.ssh/config

      - name: Download latest backup and current release record
        env:
          REMOTE: ${{ vars.DEPLOY_REMOTE }}
        run: |
          retry_network() {
            local attempt output status
            output="$(mktemp)"
            for attempt in 1 2 3 4 5; do
              if "$@" >"$output"; then
                cat "$output"
                rm -f "$output"
                return 0
              else
                status=$?
              fi
              : >"$output"
              if [ "$attempt" = 5 ]; then
                rm -f "$output"
                return "$status"
              fi
              sleep $((attempt * 5))
            done
          }
          REMOTE="${REMOTE:-root@72.60.195.46}"
          BACKUP_DIR=/var/www/arthurs-review/backups
          BACKUP_PATH="$(retry_network ssh "$REMOTE" "set -eu; latest=\$(find '$BACKUP_DIR' -maxdepth 1 -type f -name 'arthurs-review-*.tar.gz' -mmin -2160 -printf '%T@ %p\\n' | sort -nr | sed -n '1s/^[^ ]* //p'); test -n \"\$latest\"; printf '%s\\n' \"\$latest\"")"
          BACKUP_NAME="$(basename "$BACKUP_PATH")"
          [[ "$BACKUP_PATH" == "$BACKUP_DIR/$BACKUP_NAME" ]]
          [[ "$BACKUP_NAME" =~ ^arthurs-review-[0-9]{8}T[0-9]{6}Z\.tar\.gz$ ]]
          retry_network scp "$REMOTE:$BACKUP_PATH" production-backup.tar.gz
          retry_network scp "$REMOTE:/var/lib/arthurs-review/current-release.env" production-release.env
          scripts/verify-backup.sh production-backup.tar.gz

      - name: Read immutable release metadata
        run: |
          APP_IMAGE="$(sed -n 's/^RELEASE_APP_IMAGE=//p' production-release.env)"
          DEPLOY_COMMIT_SHA="$(sed -n 's/^RELEASE_COMMIT_SHA=//p' production-release.env)"
          IMAGE_DIGEST="$(sed -n 's/^RELEASE_IMAGE_DIGEST=//p' production-release.env)"
          SCHEMA_VERSION="$(sed -n 's/^RELEASE_SCHEMA_VERSION=//p' production-release.env)"
          [[ "$APP_IMAGE" == *@sha256:* ]]
          [[ "$DEPLOY_COMMIT_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$IMAGE_DIGEST" =~ ^sha256:[0-9a-f]{64}$ ]]
          [[ "${APP_IMAGE##*@}" == "$IMAGE_DIGEST" ]]
          [[ "$SCHEMA_VERSION" =~ ^[1-9][0-9]*$ ]]
          printf 'APP_IMAGE=%s\nDEPLOY_COMMIT_SHA=%s\nIMAGE_DIGEST=%s\nSCHEMA_VERSION=%s\n' \
            "$APP_IMAGE" "$DEPLOY_COMMIT_SHA" "$IMAGE_DIGEST" "$SCHEMA_VERSION" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Restore, migrate, and boot the isolated copy
        run: |
          MAINTENANCE_LOCK_HELD=1 scripts/restore-backup.sh production-backup.tar.gz \
            --image "$APP_IMAGE" \
            --expected-commit "$DEPLOY_COMMIT_SHA" \
            --expected-digest "$IMAGE_DIGEST" \
            --expected-schema "$SCHEMA_VERSION"
